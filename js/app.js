class Cells {
  constructor(id, state, coo){
    this.id=id;
    this.state=state;
    this.coo=coo;
    this.target=this.coo[4]
    this.newTarget= this.target+1;
  }
  
  checkStates (arrayCels){
    if (arrayCels.map((Cells)=>{
      for (generatedCells[this.id])
    })) return  
  }
}

const generatedCells = [];

for (let i = 0; i < 100; i++) {
  generatedCells.push(new Cells(i, false, [1,0,0,0,0,0,0,0,0]))
}



console.log(generatedCells)
